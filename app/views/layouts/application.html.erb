<!DOCTYPE html>
<html>
  <head>
    <title>ScheduleManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="wrapper">
      <header>
        <h1>Ruby on Rails スケジュール管理アプリ</h1>
      </header>
      
      <%= yield %>
    
      <footer>
        <% if current_page?(:schedules) %>
         <h1>現在の日時：<%= l(Time.now) %>   スケジュール合計：<%= Schedule.count %></h1>
        <% else %>
         <h1>現在の日時：<%= l(Time.now) %></h1>
        <% end %>
      </footer>
    </div>
  </body>
  
</html>
